# A Raytracer for Windows in C++ using SDL3

It's built with CMake, and compiled with msvc
```
cmake -B build
cmake --build build
```

It should be relatively platform/compiler agnostic. There's no windows specific APIs, and SDL3 allows for portability